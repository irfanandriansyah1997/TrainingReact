import React, { ReactElement } from 'react';

/**
 * Apps Instance
 * @returns {ReactElement}
 */
function App(): ReactElement {
    return (
        <div className="App">
            Hello World
        </div>
    );
}

export default App;
